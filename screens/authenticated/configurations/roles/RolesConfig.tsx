import { Typography } from '@components/typography'
import { ConfigurationsScreenProps } from '@typings/navigation'
import React from 'react'

export const RolesConfig = (props: ConfigurationsScreenProps<"Roles">) => {
    return (
        <Typography>Roles Config</Typography>
    )
}
